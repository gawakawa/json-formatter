name: "CI"

on:
  push:
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Install Nix
        uses: cachix/install-nix-action@v31

      - name: Format check
        run: nix fmt -- --ci

      - name: Build and test
        run: nix build

      - name: Flake check
        run: nix flake check
